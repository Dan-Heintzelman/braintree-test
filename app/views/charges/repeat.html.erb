<!-- Refactor to add change payment. Also would want to display current defauly payment credentials -->

<h4>You have a credit card securely stored on file. Click to confirm purchase for <%= number_to_currency(@total_cost, precision: 2) %> </h4>

<%= form_tag({controller: 'charges', action: 'create'}, method: 'post', id: 'braintree-form') do %>
  <input type="submit" class="btn btn-default" value="Submit Transaction">
<% end %>
